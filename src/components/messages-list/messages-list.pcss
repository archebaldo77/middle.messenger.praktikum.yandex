.messages-list {
  margin: 0;
  padding: 20px;
  list-style: none;
  height: 100vh;
  max-height: 100vh;
  overflow: scroll;

  &__item {
    display: flex;
    margin-bottom: 20px;

    &:last-child {
      margin-bottom: 0;
    }
  }
}
